import type { CSSObject } from '@mantine/styles'

export const GlobalStyles: CSSObject = {
    '*, *::before, *::after': {
        boxSizing: 'border-box',
        margin: 0,
        padding: 0
    }
}
